<div class="d-flex align-items-center justify-content-center">
	<h1>Welcome to Scheduled Tweets</h1>

  <!--% if @user %-->
<!--    Logged in as <!-%= @user.email %-->

   <!--% if Current.user %-->
    <!--Logged in as <--%= Current.use %>
    <--% link_to "logout", logout_path, method: :delete %-->
    <!--using button is symantically more correct. it generates form around this and has JS that uses hidden input to use method delete-->
    <!--%= button_to "Logout", logout_path, method: :delete %>

  <--% end %-->
</div>

<%= button_to "Connect Twitter", "/auth/twitter", method: :post, data: { turbo:false}, class: "btn btn-primary" %>
